# Instrucciones para Claude

- Cada vez que modifiques un archivo HTML (por ejemplo `prueba.html`), envíaselo al usuario en el chat como archivo descargable (SendUserFile con `display: "attach"`), además de hacer commit y push. El usuario lo descarga directamente desde el chat.
- Responde en español.
